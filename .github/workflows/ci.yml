name: CI - Build and Test

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout repositorio
        uses: actions/checkout@v4

      - name: ☕ Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17

      - name: 🧪 Construir y correr pruebas
        run: mvn clean test
        working-directory: ./pqrs  # 👈 Ajusta esto

      - name: 📊 Publicar cobertura con JaCoCo
        if: always()
        run: mvn jacoco:report
        working-directory: ./pqrs  # 👈 También aquí

      - name: ✅ Verificar calidad del código
        run: mvn checkstyle:check
        working-directory: ./pqrs  # 👈 Y aquí también
